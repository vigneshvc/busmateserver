# busmateserver
Copy the whole busmate directory into apache server htdocs folder
execute the file busmate.sql in any mysql DB server
edit dbcon.php - a file to connect the server with the backend php code if necessary
